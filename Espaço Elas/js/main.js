$('.slider-principal').slick({
    // dots: true,
    infinite: true,
    speed: 700,
    // slidesToShow: 1,
    adaptiveHeight: true,
    adaptiveWidth: true,
    centerMode: true,
    centerPadding: '170px',
    slidesToShow: 1,
    autoplay: true
});